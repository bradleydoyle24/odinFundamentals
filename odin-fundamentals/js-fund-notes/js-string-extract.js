let string = "This is a string in javascript";
let length = string.length;

let sliceString = string.slice(0, 5);

let sub = string.substr(0, 10);

alert(string);

alert(`String length is ${length}`);
alert(`slice is ${sliceString}`);
alert(`substr is ${sub}`);